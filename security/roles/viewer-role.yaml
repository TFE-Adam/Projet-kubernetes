apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: nginx-viewer
  namespace: nginx-web

rules:
- apiGroups: [""]
  resources:
  - pods
  - services
  verbs:
  - get
  - list
  - watch
